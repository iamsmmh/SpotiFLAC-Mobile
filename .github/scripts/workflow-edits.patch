diff --git a/.github/workflows/build-mobile.yml b/.github/workflows/build-mobile.yml
index 236c277..4ad919b 100644
--- a/.github/workflows/build-mobile.yml
+++ b/.github/workflows/build-mobile.yml
@@ -138,14 +138,7 @@ jobs:
       # --- Android SDK components ------------------------------------------
       - name: Install Android SDK & NDK
         run: |
-          set -euo pipefail
-          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
-          yes | "$SDKMANAGER" --licenses >/dev/null 2>&1 || true
-          "$SDKMANAGER" \
-            "ndk;${ANDROID_NDK_VERSION}" \
-            "${ANDROID_PLATFORM}" \
-            "${ANDROID_BUILD_TOOLS}"
-          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/${ANDROID_NDK_VERSION}" >> "$GITHUB_ENV"
+          bash .github/scripts/setup-android-sdk.sh
 
       # --- Bake the Go backend into an AAR --------------------------------
       - name: Build Go backend (gomobile AAR)
@@ -228,19 +221,7 @@ jobs:
 
       # --- Environment setup: Xcode, Go, Flutter ---------------------------
       - name: Select Xcode ${{ env.XCODE_VERSION }}
-        run: |
-          set -e
-          if [ -d "/Applications/Xcode_${XCODE_VERSION}.app" ]; then
-            sudo xcode-select -s "/Applications/Xcode_${XCODE_VERSION}.app"
-          elif [ -d "/Applications/Xcode_16.4.app" ]; then
-            sudo xcode-select -s /Applications/Xcode_16.4.app
-          elif [ -d "/Applications/Xcode_16.2.app" ]; then
-            sudo xcode-select -s /Applications/Xcode_16.2.app
-          else
-            sudo xcode-select -s /Applications/Xcode.app || true
-          fi
-          xcodebuild -version
-          ls /Applications/ | grep Xcode || true
+        run: bash .github/scripts/select-xcode.sh
 
       - name: Setup Go
         uses: actions/setup-go@v6
@@ -359,40 +340,14 @@ jobs:
         run: |
           set -euo pipefail
           flutter build ios --release --no-codesign --config-only
-          cd ios
-          pod install
-
-          # NOTE: Do NOT pass both -sdk iphoneos and -destination together.
-          # Xcode 16+ rejects that combination with exit code 70 and an empty log.
-          # Use -destination 'generic/platform=iOS' alone; Xcode selects the SDK.
-          LOG="$RUNNER_TEMP/xcodebuild-archive.log"
-          set +o pipefail
-          xcodebuild archive \
-            -workspace Runner.xcworkspace \
-            -scheme Runner \
-            -configuration Release \
-            -destination 'generic/platform=iOS' \
-            -archivePath "$RUNNER_TEMP/Runner.xcarchive" \
-            CODE_SIGNING_ALLOWED=NO \
-            CODE_SIGNING_REQUIRED=NO \
-            CODE_SIGN_IDENTITY="" \
-            DEVELOPMENT_TEAM="" \
-            EXPANDED_CODE_SIGN_IDENTITY="" \
-            ENABLE_USER_SCRIPT_SANDBOXING=NO \
-            2>&1 | tee "$LOG"
-          STATUS="${PIPESTATUS[0]}"
-          set -o pipefail
-
-          if [ "$STATUS" -ne 0 ]; then
-            echo "::group::xcodebuild diagnostics (last 200 lines)"
-            tail -200 "$LOG" || true
-            echo "::endgroup::"
-            echo "::group::xcodebuild errors"
-            grep -nE "error:|fatal error:|Undefined symbol|ld: |clang: error|Command .* failed" "$LOG" | head -100 || true
-            echo "::endgroup::"
-            echo "::error::xcodebuild archive failed with exit code $STATUS - see the diagnostics above and the ios-xcodebuild-log artifact"
-            exit "$STATUS"
-          fi
+          (cd ios && pod install)
+
+          # The archive itself lives in .github/scripts/archive-ios.sh: it tries
+          # -destination 'generic/platform=iOS' and falls back to -sdk iphoneos
+          # when the runner's Xcode has no installed iOS device platform.
+          bash .github/scripts/archive-ios.sh \
+            "$RUNNER_TEMP/Runner.xcarchive" \
+            "$RUNNER_TEMP/xcodebuild-archive.log"
 
       - name: Upload xcodebuild log
         if: always()
diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index ac246eb..dec1b64 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -153,12 +153,11 @@ jobs:
 
       - name: Install Android SDK & NDK
         run: |
-          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true
-          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager \
-            "ndk;29.0.14206865" \
-            "platforms;android-35" \
-            "build-tools;35.0.0"
-          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/29.0.14206865" >> "$GITHUB_ENV"
+          bash .github/scripts/setup-android-sdk.sh
+        env:
+          ANDROID_NDK_VERSION: "29.0.14206865"
+          ANDROID_PLATFORM: "platforms;android-37"
+          ANDROID_BUILD_TOOLS: "build-tools;35.0.0"
 
       - name: Build Go backend for Android
         working-directory: go_backend
diff --git a/.github/workflows/release.yml b/.github/workflows/release.yml
index 699aed7..6109d37 100644
--- a/.github/workflows/release.yml
+++ b/.github/workflows/release.yml
@@ -82,10 +82,11 @@ jobs:
 
       - name: Install Android SDK & NDK
         run: |
-          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true
-          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager \
-            "ndk;29.0.14206865" "platforms;android-35" "build-tools;35.0.0"
-          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/29.0.14206865" >> $GITHUB_ENV
+          bash .github/scripts/setup-android-sdk.sh
+        env:
+          ANDROID_NDK_VERSION: "29.0.14206865"
+          ANDROID_PLATFORM: "platforms;android-37"
+          ANDROID_BUILD_TOOLS: "build-tools;35.0.0"
 
       - name: Install gomobile
         working-directory: go_backend
@@ -173,19 +174,9 @@ jobs:
         uses: actions/checkout@v6
 
       - name: Select Xcode
-        run: |
-          set -e
-          if [ -d "/Applications/Xcode_26.1.1.app" ]; then
-            sudo xcode-select -s /Applications/Xcode_26.1.1.app
-          elif [ -d "/Applications/Xcode_16.4.app" ]; then
-            sudo xcode-select -s /Applications/Xcode_16.4.app
-          elif [ -d "/Applications/Xcode_16.2.app" ]; then
-            sudo xcode-select -s /Applications/Xcode_16.2.app
-          else
-            sudo xcode-select -s /Applications/Xcode.app || true
-          fi
-          xcodebuild -version
-          ls /Applications/ | grep Xcode
+        run: bash .github/scripts/select-xcode.sh
+        env:
+          XCODE_VERSION: "26.1.1"
 
       - name: Setup Go
         uses: actions/setup-go@v6
@@ -274,23 +265,12 @@ jobs:
 
       - name: Build iOS (unsigned)
         run: |
+          set -euo pipefail
           flutter build ios --release --no-codesign --config-only
-          cd ios
-          pod install
-          # NOTE: Do NOT pass both -sdk iphoneos and -destination together.
-          # Xcode 16+ rejects that combination with exit code 70 and an empty log.
-          # Use -destination 'generic/platform=iOS' alone.
-          xcodebuild -workspace Runner.xcworkspace \
-            -scheme Runner \
-            -configuration Release \
-            -destination 'generic/platform=iOS' \
-            -archivePath build/Runner.xcarchive \
-            archive \
-            CODE_SIGNING_ALLOWED=NO \
-            CODE_SIGNING_REQUIRED=NO \
-            CODE_SIGN_IDENTITY="" \
-            DEVELOPMENT_TEAM="" \
-            ENABLE_USER_SCRIPT_SANDBOXING=NO
+          (cd ios && pod install)
+          bash .github/scripts/archive-ios.sh \
+            "$PWD/ios/build/Runner.xcarchive" \
+            "${RUNNER_TEMP}/xcodebuild-archive.log"
 
       - name: Create IPA
         run: |
diff --git a/.github/workflows/unsigned-release.yml b/.github/workflows/unsigned-release.yml
index 218429b..621d593 100644
--- a/.github/workflows/unsigned-release.yml
+++ b/.github/workflows/unsigned-release.yml
@@ -134,11 +134,7 @@ jobs:
 
       - name: Install Android SDK components
         run: |
-          set -euo pipefail
-          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
-          yes | "$SDKMANAGER" --licenses >/dev/null 2>&1 || true
-          "$SDKMANAGER" "ndk;${ANDROID_NDK_VERSION}" "${ANDROID_PLATFORM}" "${ANDROID_BUILD_TOOLS}"
-          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/${ANDROID_NDK_VERSION}" >> "$GITHUB_ENV"
+          bash .github/scripts/setup-android-sdk.sh
 
       - name: Build Go backend (gomobile AAR)
         working-directory: go_backend
@@ -214,19 +210,7 @@ jobs:
         uses: actions/checkout@v6
 
       - name: Select Xcode ${{ env.XCODE_VERSION }}
-        run: |
-          set -e
-          if [ -d "/Applications/Xcode_${XCODE_VERSION}.app" ]; then
-            sudo xcode-select -s "/Applications/Xcode_${XCODE_VERSION}.app"
-          elif [ -d "/Applications/Xcode_16.4.app" ]; then
-            sudo xcode-select -s /Applications/Xcode_16.4.app
-          elif [ -d "/Applications/Xcode_16.2.app" ]; then
-            sudo xcode-select -s /Applications/Xcode_16.2.app
-          else
-            sudo xcode-select -s /Applications/Xcode.app || true
-          fi
-          xcodebuild -version
-          ls /Applications/ | grep Xcode
+        run: bash .github/scripts/select-xcode.sh
 
       - name: Setup Go
         uses: actions/setup-go@v6
@@ -329,40 +313,14 @@ jobs:
         run: |
           set -euo pipefail
           flutter build ios --release --no-codesign --config-only
-          cd ios
-          pod install
-
-          # NOTE: Do NOT pass both -sdk iphoneos and -destination together.
-          # Xcode 16+ rejects that combination with exit code 70 and an empty log.
-          # Use -destination 'generic/platform=iOS' alone; Xcode selects the SDK.
-          LOG="$RUNNER_TEMP/xcodebuild-archive.log"
-          set +o pipefail
-          xcodebuild archive \
-            -workspace Runner.xcworkspace \
-            -scheme Runner \
-            -configuration Release \
-            -destination 'generic/platform=iOS' \
-            -archivePath "$RUNNER_TEMP/Runner.xcarchive" \
-            CODE_SIGNING_ALLOWED=NO \
-            CODE_SIGNING_REQUIRED=NO \
-            CODE_SIGN_IDENTITY="" \
-            DEVELOPMENT_TEAM="" \
-            EXPANDED_CODE_SIGN_IDENTITY="" \
-            ENABLE_USER_SCRIPT_SANDBOXING=NO \
-            2>&1 | tee "$LOG"
-          STATUS="${PIPESTATUS[0]}"
-          set -o pipefail
-
-          if [ "$STATUS" -ne 0 ]; then
-            echo "::group::xcodebuild diagnostics (last 200 lines)"
-            tail -200 "$LOG" || true
-            echo "::endgroup::"
-            echo "::group::xcodebuild errors"
-            grep -nE "error:|fatal error:|Undefined symbol|ld: |clang: error|Command .* failed" "$LOG" | head -100 || true
-            echo "::endgroup::"
-            echo "::error::xcodebuild archive failed with exit code $STATUS - see the diagnostics above and the ios-xcodebuild-log artifact"
-            exit "$STATUS"
-          fi
+          (cd ios && pod install)
+
+          # The archive itself lives in .github/scripts/archive-ios.sh: it tries
+          # -destination 'generic/platform=iOS' and falls back to -sdk iphoneos
+          # when the runner's Xcode has no installed iOS device platform.
+          bash .github/scripts/archive-ios.sh \
+            "$RUNNER_TEMP/Runner.xcarchive" \
+            "$RUNNER_TEMP/xcodebuild-archive.log"
 
       - name: Upload xcodebuild log
         if: always()
